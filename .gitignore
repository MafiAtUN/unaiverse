# Secrets
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store

# Tooling
.deepeval/

# Build
dist/
.astro/
node_modules/

# Copied out of node_modules on every dev/build (scripts/vendor-timeline.mjs).
# package.json is the source of truth for the TimelineJS version, not this copy.
public/vendor/

# Local infra identifiers — keep out of the public repo (see VERIFICATION_LOG / Phase 2 notes)
.env.example

# Playwright MCP scratch output
.playwright-mcp/
