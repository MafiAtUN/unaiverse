## What changed, for the reader

<!-- One or two sentences in plain English. The same voice as the commit log:
     "Move the Nerd Lab credit into the bar, and drop the build credit". -->

## Why

<!-- Link the issue if there is one. -->

## What you checked

<!-- Tick what applies. Delete the rest. Say what you actually ran, not what
     you believe to be true. -->

- [ ] `npm run build` then `npm test` (in that order, several assertions read `dist/`)
- [ ] `npm run check`
- [ ] Looked at it on a narrow screen
- [ ] Keyboard reachable, focus visible
- [ ] Contrast computed, not eyeballed, on any new text pairing
- [ ] Every factual claim keeps its source link
- [ ] No em dashes in anything the site renders
- [ ] No new document symbol outside `content/learn/voice/approved-references.json`
- [ ] Added a dependency? Ran `npm run relock`, not just `npm install`

## Anything a reviewer should know

<!-- Trade-offs, things you were unsure about, things deliberately left out. -->
